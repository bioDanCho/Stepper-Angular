import { ActiveStepDirective } from './active-step.directive';

describe('ActiveStepDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveStepDirective();
    expect(directive).toBeTruthy();
  });
});
